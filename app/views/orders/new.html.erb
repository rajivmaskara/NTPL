<%= link_to "Add Customer", new_customer_path %> | <%= link_to "Add Product Category", new_category_path %>

<%= form_for @order do |f| %>
	
	<%= f.label :Customer %><br/>
	<%= f.collection_select :customer_id, Customer.all, :id, :customer_name, prompt: "Select Customer" %><br/>
	
<%end%>